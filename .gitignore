# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Legacy full-RMS modules removed from the BonManzE build (kept on disk here
# only because this environment can't delete files outright). Safe to delete
# this whole folder manually.
_deleted_modules/

# Firebase Local Emulator Suite's saved state (npm run emulators exports here
# on exit and re-imports on next start, so the bootstrapped Owner
# role/account and any other emulator data survive a restart). Local dev
# data only — never meant to be committed.
.emulator-data/
